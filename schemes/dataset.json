{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "title": "Dataset",
  "description": "Describes a dataset, and gives associated bibliographic informations. Datasets are a collection of networks, published in one or several studies.",
  "type": "object",
  "properties": {
    "id": {
      "description": "Access code for the dataset in the local database.",
      "type": "string"
    },
    "name": {
    	"description": "Name of the dataset",
      "type": "string"
    },
    "description": {
       "description": "Some additional informations about the dataset.",
       "type": "string"
    },
    "papers": {
       "description": "Informations on the papers describing the dataset.",
       "type": "array",
       "items": {
          "type": "string"
       }
    },
    "data": {
       "description": "Informations on the data (can be the DOI of the dataset, or identifiers on where the dataset was published)",
       "type": "array",
       "items": {
          "type": "string"
       }
    },
    "networks": {
       "description": "List of networks ID",
       "type": "array",
       "items": {
          "type": "string"
       },
       "uniqueItems": true
    }
  },
  "required": ["name", "networks"]
}
