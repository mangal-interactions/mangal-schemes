{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "title": "Trait value",
  "description": "Value of one trait for a sampled population",
  "type": "object",
  "properties": {
    "id": {
      "description": "Access code for the trait value in the local database",
      "type": "string"
    },
    "name": {
    	"description": "Trait name",
      "type": "string"
    },
    "value": {
       "description": "Trait value",
       "type": "number"
    },
    "units": {
       "description": "Units",
       "type": "string"
    },
    "description": {
       "description": "Additional details on the trait measurement",
       "type": "string"
    }
  },
  "required": ["name", "value", "units"]
}
