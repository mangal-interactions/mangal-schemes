{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "title": "Population object",
  "description": "General information about a population, and the taxa it belongs to",
  "type": "object",
  "properties": {
    "id": {
      "description": "Access code for the population in the local database",
      "type": "string"
    },
    "taxa": {
       "description": "Access code for the taxa to which this population belongs",
       "type": "string"
    },
    "name": {
    	"description": "Name of the population",
      "type": "string"
    },
    "description": {
       "description": "Some additional informations about the population",
       "type": "string"
    }
  },
  "required": ["id", "name", "taxa"]
}
